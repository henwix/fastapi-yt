services:
  backend:
    container_name: fastapi-yt-backend-dev
    build:
      context: ..
      dockerfile: Dockerfile
    image: fastapi-yt-backend-dev
    pull_policy: build
    ports:
      - "${FASTAPI_PORT}:8000"
    volumes:
      - ..:/src/
    env_file:
      - ../.env
    command: "uvicorn --factory app.presentation.api.main:create_app --host 0.0.0.0 --port 8000 --reload"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  taskiq:
    container_name: fastapi-yt-taskiq-dev
    image: fastapi-yt-backend-dev
    pull_policy: build
    command: taskiq worker app.infrastructure.taskiq.setup:broker app.infrastructure.taskiq.tasks --reload --no-configure-logging
    volumes:
      - ..:/src/
    env_file:
      - ../.env
    depends_on:
      redis:
        condition: service_healthy
